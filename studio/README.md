# Lafkos content studio

The public site is ready to consume Sanity documents through the contracts in
`src/lib/content.ts`. Create a Sanity Studio here with the Place, Trail, Story,
Category, and Site Settings schemas when the project receives its Sanity
project ID. The first public build intentionally uses the local seed dataset so
the experience remains previewable without credentials.
